## 使用volatile修饰变量的目的是什么

在 C 和 C++ 中，volatile 关键字用于修饰变量，告诉编译器该变量可能会在程序的正常代码路径之外被修改，例如通过硬件或其他线程。其主要目的是防止编译器对该变量进行优化，确保程序每次访问该变量时都从内存中读取最新值，而不是使用缓存值。

volatile 的作用

1. 防止编译器优化：

通常，编译器会基于程序逻辑优化变量的访问，比如将变量的值缓存到寄存器中。如果一个变量被标记为 volatile，编译器会放弃这种优化。
每次读取或写入 volatile 变量时，编译器都会直接访问内存，而不是寄存器。2. 处理外部修改：

volatile 通常用于表示可能被外部因素修改的变量，例如：
硬件寄存器（比如设备驱动程序中的内存映射 I/O）。
信号处理函数中被修改的全局变量。
多线程程序中被另一个线程修改的变量。

## 怎么记笔记

知识本来就是网状的，想要组织成线性的很难
碎片化的笔记其实很合理

## 为什么系统调用会被信号打断

运行在内核态的程序会被调度程序调度吗

## 条件变量解决的是什么问题

替换了哪段忙等代码段
